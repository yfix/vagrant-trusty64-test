---
- hosts: all
  sudo: yes

  pre_tasks:
    - name: Update apt cache if needed.
      apt: update_cache=yes cache_valid_time=3600

  roles:
    - yfix.common
#    - geerlingguy.nodejs
#    - geerlingguy.ruby
#    - yfix.composer
    - yfix.mc
#    - yfix.memcached
    - yfix.mysql
    - yfix.nginx
#    - yfix.ntp
    - yfix.php
    - yfix.php-mysql
#    - yfix.php-pecl
#    - yfix.php-xdebug
#    - yfix.php-xhprof
#    - yfix.postfix
#    - yfix.security

  post_tasks: []
